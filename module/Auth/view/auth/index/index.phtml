<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, sans-serif;
    }

    body {
        height: 100vh;
        background: linear-gradient(135deg, #2f6f3e, #3e8f4f);
        display: flex;
        justify-content: center;
        align-items: center;
    }

    /* Fondo con "líneas tipo circuito" */
    .background {
        position: absolute;
        width: 100%;
        height: 100%;
        background-image: radial-gradient(rgba(255, 255, 255, 0.15) 1px, transparent 1px);
        background-size: 40px 40px;
        opacity: 0.3;
    }

    /* Contenedor */
    .login-container {
        position: relative;
        text-align: center;
    }

    /* Card */
    .login-card {
        background: #fff;
        padding: 30px;
        width: 350px;
        border-radius: 10px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }

    /* Logo */
    .logo {
        margin-bottom: 20px;
        color: #333;
    }

    /* Inputs */
    form label {
        display: block;
        text-align: left;
        margin: 10px 0 5px;
        font-size: 14px;
    }

    form input[type="email"],
    form input[type="password"] {
        width: 100%;
        padding: 10px;
        border: none;
        background: #e9edf3;
        border-radius: 6px;
        margin-bottom: 10px;
    }

    /* Contraseña fila */
    .password-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .password-row a {
        font-size: 12px;
        color: #2e8b57;
        text-decoration: none;
    }

    /* Checkbox */
    .remember {
        display: flex;
        align-items: center;
        gap: 5px;
        margin: 10px 0;
        font-size: 14px;
    }

    /* Botón */
    button {
        width: 100%;
        padding: 12px;
        background: #3e8f4f;
        color: white;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-weight: bold;
        transition: 0.3s;
    }

    button:hover {
        background: #2f6f3e;
    }

    /* Footer */
    footer {
        margin-top: 20px;
        color: #fff;
        font-size: 12px;
    }
</style>

<div class="background"></div>

<div class="login-container">
    <div class="login-card">
        <h2 class="logo">Victor Pinto</h2>

        <form method="post">
            <label>Usuario *</label>
            <input type="email" name="email" placeholder="usuario@email.com" required>

            <div class="password-row">
                <label>Contraseña *</label>
                <a href="#">¿Olvidaste tu contraseña?</a>
            </div>

            <input type="password" name="password" placeholder="********" required>
       
            <?php if (!empty($this->error)): ?>
                <div style="color:red; margin-bottom:10px;">
                    <?= $this->error ?>
                </div>
            <?php endif; ?>
            
            <div class="remember">
                <input type="checkbox" id="remember">
                <label for="remember">Recordar</label>
            </div>

            <button type="submit">Iniciar sesión</button>
        </form>
    </div>

    <footer>
        <p>© Copyright 2026 Victor Pinto</p>
    </footer>
</div>